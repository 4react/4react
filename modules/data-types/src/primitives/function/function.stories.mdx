import { isFunction as isFunctionImpl } from '@4react/data-types'
import { ArgsTable, Meta, Source, Story } from '@storybook/addon-docs/blocks'
import { arg } from '../../../../../.storybook/utils/arg'
import { control } from '../../../../../.storybook/utils/control'
import { Method } from '../../../../../.storybook/utils/Method'

<Meta title="data-types/Primitives/Function" />

# Function

Utilities for the usage of function data.

## isFunction

<Source
  language="jsx"
  code="import { isFunction } from '@4react/data-types'"
/>

<Story
  name="isFunction"
  argTypes={{
    value: control(arg('value', true, 'any', 'The value to check'), 'text', 'function() { return 123 }')
  }}
>
  {(args) => (
    <Method
      name="isFunction"
      impl={isFunctionImpl}
      params={[args.value]}
    />
  )}
</Story>

### Params

<ArgsTable story="isFunction" />
