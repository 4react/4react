import { isNumber as isNumberImpl } from '@4react/data-types'
import { ArgsTable, Meta, Source, Story } from '@storybook/addon-docs/blocks'
import { arg } from '../../../../../.storybook/utils/arg'
import { control } from '../../../../../.storybook/utils/control'
import { Method } from '../../../../../.storybook/utils/Method'

<Meta title="data-types/Primitives/Number" />

# Number

Utilities for the usage of numeric data.

## isNumber

<Source
  language="jsx"
  code="import { isNumber } from '@4react/data-types'"
/>

<Story
  name="isNumber"
  argTypes={{
    value: control(arg('value', true, 'any', 'The value to check'), 'text', '123')
  }}
>
  {(args) => (
    <Method
      name="isNumber"
      impl={isNumberImpl}
      params={[args.value]}
    />
  )}
</Story>

### Params

<ArgsTable story="isNumber" />
